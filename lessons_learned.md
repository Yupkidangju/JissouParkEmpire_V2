# 실장석 공원 제국 - 기술 부채 및 교훈 (lessons_learned.md)

> **문서 버전**: v1.8.9
> **마지막 갱신**: 2026-05-31
> **상태**: 지속 갱신
> **표준**: `AI_IMPLEMENTATION_DOC_STANDARD.md` 및 `analyst.md` 파생

---

## 1. 기술 부채 (Tech Debt)

### 1.1 코드 복잡도
| 위치 | 현상 | 위험도 | 원인 |
|------|--------|--------|------|
| `app/game_engine.py` | 1628줄, `process_turn`에 15개 단계 함수 호출 | 중간 | 잔혹 컨텐츠 추가 시 계속 확장됨. 단계 함수는 이미 분리되어 있으나 파일 자체가 거대함. |
| `app/routes/game_routes.py` | 1286줄, 교역/외교/랭킹/정찰/알림이 한 파일 | 중간 | 기능 추가 시 계속 확장. 교역/외교 분리 블루프린트 고려 필요. |
| `app/static/css/style.css` | 1508줄, 미디어 쿼리 중복 완화 | 낮음 | 768px/480px 브레이크포인트 중복 CSS를 Tailwind CDN으로 대체 진행. |
| `app/templates/dashboard.html` | 746줄, 인라인 스타일 90% 이상 제거 | 낮음 | [v1.7.0] 기존 50여 개의 `style="..."` 하드코딩 인라인 스타일을 Tailwind 유틸리티 클래스로 추출 및 최적화 완료. |

### 1.2 미구현/미흡 기능
| 기능 | 위치 | 상태 | 영향 |
|------|------|------|------|
| 밀사 파견 UI | 대시보드 | API는 존재하나 버튼 없음 | 플레이어가 밀사 기능을 모름 |
| 질병 치료 UI | 대시보드 | API 존재, 버튼 없음 | 플레이어가 질병 치료 불가 (수동 DB 조작 필요) |
| 게임오버 원인 상세 | `gameover.html` | "멸망"만 표시 | 쿠데타/기아/전투별로 원인 표시 필요 |
| 전투 실시간 로그 | `battle_logs.html` | 텍스트만 | 시각적 타임라인/차트 부재 |
| 통계/그래프 | 없음 | 미구현 | 플레이어 성장 곡선 확인 불가 |

### 1.3 테스트 부재
| 영역 | 부재 현상 | 위험도 |
|------|----------|--------|
| 단위 테스트 | pytest 미설치, 자동화 테스트 0개 | 높음 |
| 밸런스 시뮬레이션 | 100턴/1000턴 시뮬레이션 스크립트 없음 | 중간 |
| 부하 테스트 | 동시 접속자 성능 측정 없음 | 낮음 (단일 서버) |
| UI 테스트 | 브라우저 자동화 없음 | 낮음 |

---

## 2. 보안 Exploit 히스토리

### 2.1 차단 완료된 Exploit

| 버전 | Exploit | 설명 | 대응 |
|------|---------|------|------|
| v1.5.0 | 저실장 무한 번식 | 운치굴 없이 저실장 무한 출산 | `baby_cap` 검사 + `BASE_BABY_CAP = 5` 추가 |
| v1.5.0 | 음수 자원 역전 | 교역/도살에 음수 입력으로 자원 증식 | `max(0, value)` 적용, `@validates` 클램핑 |
| v1.5.0 | 교역 Double Spend | 동시 수락으로 자원 2배 획득 | 원자적 `UPDATE-WHERE` + status 전환 |
| v1.5.0 | XSS (Toast/정찰) | `<script>` 주입으로 세션 탈취 | `escapeHtml()`, `textContent` 적용 |
| v1.5.0 | 디버그 백도어 | `/debug/next-turn` 무한 턴 | `DEBUG` 모드 가드 추가 |
| v1.5.1 | 보스 단독 무손실 파밍 | 보스만 출전해 승리, 피해 0 | 보스 단독 시 전투력 30% 패널티 + 승리해도 HP 3~8 감소 |
| v1.5.1 | 교역 에스크로 누락 | 제안 시 자원 미차감 → 수락 시 자원凭空 생성 | 선차감(Escrow) 도입 |
| v1.6.1 | 외교 0AP 무한 반복 | 적대 선언(무료) → 침공 → 해제(무료) 반복 | 적대 선언/해제에 1AP 추가 |
| v1.6.1 | Soft-Lock | AP=0 시 버튼 비활성화 → consume_turn 호출 불가 | disabled 조건을 `AP < N AND turn_quota < 1` 복합 조건으로 변경 |
| v1.6.2 | 소수점 불사 부대 | `int(4 * 0.2) = 0` → 4명 이하 출정 시 사상자 0 | `stochastic_round()` 확률적 반올림 도입 |
| v1.6.2 | 교역소 DoS | 무제한 pending 교역 생성 | 유저당 10개 제한, 쿼리 limit(50) |
| v1.6.3 | 거절의 블랙홀 | 교역 거절 시 에스크로 환불 누락 → 자원 소멸 | `trade_reject`에 환불 로직 추가 |
| v1.6.3 | 취소 중복 환불 | 동시 취소 시 여러 번 환불 | 원자적 `UPDATE-WHERE` 상태 전환 |
| v1.8.0 | 보호 모드 Lost Update | 대시보드 로드(GET) 시 비동기 POST 행동에 의한 DB 변경을 메모리 구버전 데이터로 덮어씀 | `check_and_enter_protection` 시작 부분에 `with_for_update()` 락 및 refresh 강제 적용 |
| v1.8.0 | 재시작 무한 리다이렉트 | 공원 삭제와 생성 트랜잭션 분리로 인한 공원 유실 및 로그인/대시보드 간의 무한 리다이렉트 핑퐁 | restart 단일 트랜잭션화 및 로그인/루트/대시보드 진입 시 자동 복구 가드 탑재 |
| v1.8.1 | Gunicorn 락 우회 | 멀티 프로세스 환경에서 threading.Lock 무력화로 교역 제한 및 NPC 턴 중복 진행 우회 | DB 비관적 락(with_for_update) 정렬 획득 및 turn_count 비교 가드 도입 |
| v1.8.1 | NPC 턴 Lost Update | 메모리 수정(passive_growth)과 범용 엔진 commit의 혼용으로 플레이어의 공격 무산 및 롤백 붕괴 | passive_growth의 case() 원자적 UPDATE 전환 및 commit=False 옵션 도입으로 중간 커밋 제거 |
| v1.8.2 | AP 누수 (AP Leakage) | `consume_turn()` 선행 커밋 이후 실제 행동 검증 실패 또는 전투 무산 시 AP 증발 | `game_engine.refund_ap` 공용 보상 트랜잭션 헬퍼 도입 및 각 행동 라우트 실패 시 환불 작동 설계 |
| v1.8.3 | 좀비 상태 (Zombie TOCTOU) | 턴 소모 중 멸망한 공원의 건설/침공 등 잔여 행동 강행 및 비관적 락 획득 후 상대방 멸망 무시 교역/전투 | 턴 진행 후 즉시 멸망 여부 재검사 가드 추가 및 trade_accept/execute_battle 락 획득 직후 멸망 상태 재확인 도입 |
| v1.8.4 | Cascade Delete 에스크로 유실 | 상대방 공원 삭제(재시작) 시 교역 및 밀사 에스크로 자원/유닛이 환불 없이 소멸 | `before_delete` DB 이벤트 리스너를 도입하여 pending 거래 자원 및 active 밀사 유닛 원자적 자동 환불 처리 |
| v1.8.5 | 교차 외교 중복 관계 및 모순 상태 | 동시 교차 외교 요청 시 중복 관계가 다수 생성되고, 적대 선언 시 단일 갱신으로 인해 동맹이자 적대인 모순 상태 잔존 | Canonical Ordering으로 ID 순서 강제화, ID 정렬 2중 락 및 일괄 갱신 Bulk Update로 중복/모순 상태 원천 차단 |
| v1.8.6 | NPC 턴 분할 트랜잭션 락 유실 및 Lost Update | NPC 일괄 턴 동기화 시 루프 내부 commit으로 락이 조기 풀려 타 프로세스가 개입하고 Stale 객체가 Auto-Flush되며 상태를 덮어씀 | 루프 외부 ID 목록 추출 후 개별 루프 트랜잭션 단위 비관적 락 조회(`with_for_update()`) 격리 도입 |
| v1.8.7 | 교역 거절 IDOR | 타인의 교역 제안 ID로 거절 POST API를 요청하여 비공개/공개 교역을 강제 거절 차단(DoS) | trade_reject 원자적 UPDATE 조건식에 receiver_id == park.id 가드 필터 강제 |
| v1.8.7 | 시장 좀비 거래 노출 | 멸망한 발송자의 교역 제안이 시장에 계속 남아 쿼터와 트랜잭션 리소스 낭비 | trade_market 쿼리에 Park 조인 및 is_destroyed == False 필터링 선제 정화 |
| v1.8.8 | 턴 동기화 갭 AP 복제 | 턴 진행 후 무락 갭 동안 다중 요청이 차감한 AP 정보를 Stale AP 데이터로 덮어써 AP 소실 우회 | 최종 AP 감산 직전 플레이어 공원 2차 비관적 락 및 refresh 강제 적용 |
| v1.8.9 | NPC Savepoint 파괴 | NPC 공격 시 내부 commit에 의해 세이브포인트가 영구 파괴되어 `ResourceClosedError` 및 AP 무한 미소모 루프 폭사 유발 | 내부 commit을 `db.session.flush()`로 전환하여 세이브포인트를 깨뜨리지 않고 SQL만 방출하며, nested 롤백 실패 시 `db.session.rollback()` 예외 가드 마련 |
| v1.8.9 | AP 환불 블랙홀 (AP Blackhole) | 예외/기각 상황에서 AP 복구를 기동하지만, 최종 `commit`이 결여되어 리다이렉트 시 AP가 유실됨 | 환불을 실행하는 라우터 내 모든 예외 기각 분기 뒤에 즉각적인 `db.session.commit()` 강제 |
| v1.8.9 | 밀사 복귀 Lost Update | 밀사 복귀 시 overcrowding 연산을 비관적 락 없이 처리하여 concurrent 요청(채집/교역)에 의한 DB 상태 변경을 덮어씀 | overcrowding 처리 진입 직전에 다시 `with_for_update()` 비관적 락 확보 및 `refresh()` 동기화 적용 |
| v1.8.9 | NPC 공격 데드락 (DEADLOCK-F005) | NPC 턴 시작 시 획득한 비관적 락으로 인해 execute_battle 호출 시 락 순서 역전(Lock Order Inversion)에 의한 영구 교착 상태 발생 | process_npc_turn 최상단 비관적 락을 영구 해제하고 단순 refresh만 전개하며, 턴 동기화 스케줄러(_sync_npc_turns)에서 NPC 기본 턴 처리(process_turn) 완료 즉시 commit으로 선점 락을 소멸시킨 후 process_npc_turn()을 독립된 트랜잭션 하에 구동하는 2단계 트랜잭션 경계 분리 구조 채택 |
| v1.8.9 | XSS 헬퍼 교차 검증 및 정적 innerHTML 안전성 스캔 한계 | XSS 회귀 테스트가 실제 브라우저 DOM 삽입 경로를 모사하는 E2E 레벨이 아니어서 동적 HTML 렌더링 시의 누설 가능성을 완전히 예방하기 어려움 | 실제 `game.js` 파일에서 `escapeHtml` 함수를 추출하여 Node.js `v24.14.1` 런타임상에서 교차 확인하고, `game.js` 소스 코드 전체를 regex 정적 분석하여 `innerHTML` 대입문 내에서 `escapeHtml` 호출이 누락되었는지를 자동 검출하는 정밀한 결합형 회귀 테스트 수립 |
| v1.8.9 | SQLite 동시성 검증 한계 | in-memory SQLite의 비관적 락 no-op으로 인해 multi-session Lost Update 발생 가능성을 단위 테스트 스위트에서 재현하지 못함 | `sqlalchemy.orm.sessionmaker`를 이용해 두 독립된 세션을 띄우고 stale read 후 write-back 경합 시 `session.refresh`를 통해 최종 일관성을 회복하는 실질적 Race Condition 회귀 테스트 설계 |
| v1.8.9 | 개발 서버 LAN 노출 | `run.py` 기동 시 default host가 `0.0.0.0`으로 잡혀 같은 대역 네트워크 해커에게 디버거 콘솔 백도어가 노출됨 | 기본 바인딩 host를 `127.0.0.1`로 엄격하게 락다운하고 `FLASK_RUN_HOST` 환경변수 주입을 통해서만 opt-in 기동하도록 구조 개편 |
| v1.8.9 | 프로덕션 디버그 이탈 | 운영자 설정 실수로 `FLASK_ENV=production`인데 `DEBUG=true`를 활성화해 debug backend 및 임시 fallback 키가 노출됨 | 명시적 프로덕션 모드 감지 시 DEBUG를 강제로 비활성화(False) 처리하고 시크릿 키 누락 시 Fail-Closed ValueError로 중단하는 하드 락다운 가드 마련 |

### 2.2 잠재적 위험 (아직 차단되지 않음)

| 위험 | 설명 | 가능성 | 대응 방안 |
|------|------|--------|----------|
| 세션 하이재킹 | Flask 기본 세션 쿠키 탈취 | 낮음 | HTTPS 배포, 세션 타임아웃 설정 |
| 비밀번호 브루트포스 | 로그인 시도 무제한 | 중간 | 로그인 시도 제한(Rate Limit) 미구현 |
| NPC 이름 중복 충돌 | 동시 서버 시작 시 NPC 초기화 Race | 낮음 | `_init_npc_parks`가 이미 존재 여부 확인 |
| 스케줄러 중복 실행 | Gunicorn 워커 2개 시 스케줄러 2개 | 중간 | `replace_existing=True`로 완화, 완전 차단은 별도 락 필요 |
| DB 파일 손상 | SQLite 쓰기 중 크래시 | 낮음 | 주기적 백업 권장 (BUILD_GUIDE.md 참조) |

---

## 3. 버그 패턴

### 3.1 반복되는 버그 유형
| 패턴 | 발생 파일 | 원인 | 예방책 |
|------|----------|------|--------|
| AP 체크 누락 | `game_routes.py` | 새 라우트 추가 시 `consume_turn` 호출 잊음 | 코드 리뷰 체크리스트에 `consume_turn` 필수 항목 추가 |
| 음수 계산 | `game_engine.py` | 감산 시 보유량 미검증 | `@validates` 클램핑으로 안전망 유지 |
| int() 절사 | `battle_engine.py` | 소수부 버림으로 Exploit | `stochastic_round()` 패턴 표준화 |
| DB 객체 동기화 누락 | `game_routes.py` | 원자적 UPDATE 후 `db.session.refresh()` 미호출 | 에스크로 패턴 사용 시 refresh 필수 |
| XSS | 템플릿/JS | 사용자 입력을 `innerHTML`에 삽입 | `textContent`/`escapeHtml()` 기본 사용 |

### 3.2 주요 버그 수정 이력
| 버전 | 버그 | 영향 | 수정 내용 |
|------|------|------|----------|
| v1.5.1 | 교역 수락 로직 오류 | 발송자 이중 감산 | 에스크로 반영으로 재확인 제거 |
| v1.6.0 | 교역 연산 순서 오류 | 자원 증발 (cap 잘림 후 빼기) | 뺄셈 먼저, 덧셈 나중으로 순서 변경 |
| v1.6.0 | Ghost AP 버그 | 매 행동마다 턴 진행 → AP 무력화 | `consume_turn` 래퍼로 AP/턴 분리 |
| v1.6.1 | Soft-Lock | AP=0 시 버튼 비활성화 → 턴 진행 불가 | disabled 조건 복합화 |
| v1.6.3 | 외교 0AP 무한 반복 | 적대/해제 무료 반복 | 1AP 소비 추가 |
| v1.8.0 | 보호 모드 Lost Update 및 재시작 무한 루프 | 동시성 Lost Update 및 공원 유실로 인한 로그인 차단 | 비관적 락 도입 및 통합 복구 헬퍼 적용 |
| v1.8.1 | Gunicorn 락 우회 및 NPC 턴 Lost Update | 다중 프로세스 환경 동시성 제어 실패 및 롤백 무결성 붕괴 | with_for_update() 직렬화, case() 자연 성장 및 commit=False 매개변수 도입 |
| v1.8.2 | 행동 검증 실패 시 AP 누수 | 분할 트랜잭션 선행 커밋 이후 후속 로직 실패 시 AP 증발 및 누수 | `game_engine.refund_ap` 보상 트랜잭션 헬퍼 설계 및 각 행동 라우트 실패 분기에 연동 |
| v1.8.3 | 멸망 후 좀비 행동 허용 | 턴 도중 공원이 멸망한 상태에서 행동을 취하거나, 락 획득 대기 도중 상대방이 멸망한 상태에서 교약/전투 성사 | consume_turn 내 process_turn 직후 is_destroyed 가드 추가 및 trade_accept/execute_battle 락 획득 후 멸망 상태 재확인 도입 |
| v1.8.4 | Cascade Delete 자원/유닛 누수 | 상대방 공원 삭제로 교역/밀사가 Cascade 삭제될 때 에스크로 자원 및 유닛 환불 유실 | models.py 최하단에 before_delete DB 이벤트 리스너를 추가하여, pending 자원 환불(cap 보정) 및 active 성체 환불 자동화 |
| v1.8.5 | 교차 외교 중복 생성 및 관계 해제 누락으로 인한 모순 | 동맹과 적대 관계가 동시에 중복 존재하게 되어 전투 및 외교가 좀비화 및 Soft-Lock되는 현상 | ID 정렬 2중 락으로 트랜잭션 직렬화, Canonical Ordering 순서 매핑 및 일괄 bulk update로 중복 해제 보완 |
| v1.8.6 | NPC 턴 중간 커밋 락 해제 및 AI 행동 예외 시 락 유실 | 일괄 동기화 트랜잭션 쪼개짐에 따른 락 조기 해제 및 AI 예외 롤백 시 전체 락 소실과 스탬피드 유발 | 개별 트랜잭션 단위 비관적 락 확보, Nested Savepoint 예외 가드 및 밀사 2-Way Lock 격리 적용 |
| v1.8.7 | 교역 거절 IDOR 및 좀비 거래 | 타인의 교역을 마음대로 거절 폭파하거나 멸망한 유저의 교역이 시장에 노출되어 리소스 및 UI 점유 | trade_reject UPDATE에 receiver_id 일치 조건 강제 및 trade_market 쿼리에 Park JOIN 및 is_destroyed == False 가드 탑재 |
| v1.8.8 | 슬로우 패스 AP 덮어쓰기 및 복제 | 턴 스케줄러 동기화 도중 락이 해제되어 비동기 요청이 AP를 깎아도 메모리 구버전 데이터로 덮어씀 | _sync_npc_turns 완료 후 최종 감산 전 플레이어 공원에 대해 다시 with_for_update() 및 refresh() 적용 |
| v1.8.9 | NPC 공격 시 트랜잭션 붕괴 | NPC 루프 정지, AP 무한 복제 | commit 대신 flush 사용 및 2차 롤백 예외 방어로 고도 예방 |
| v1.8.9 | AP 환불 유실 버그 | 멸망/기각 시 1AP 영구 누수 | 라우터 단에서 즉시 명시적 commit을 수행하여 수치 보존 |
| v1.8.9 | 밀사 overcrowding 자원/인구 덮어쓰기 | 채집/교역 변경점 소멸 or 복제 | 연산 직전 `with_for_update()` 락 및 refresh로 데이터 덮어쓰기 레이스 차단 |
| v1.8.9 | NPC 공격 락 순서 역전 데드락 | 서비스 장애 및 DB 커넥션 풀 고갈 | 최상단 비관적 락 제거 후 execute_battle 내 Canonical Locking에 위임하여 고도 예방 |

---

## 4. 성능 이슈

### 4.1 현재 상태
| 항목 | 현상 | 원인 | 우선순위 |
|------|------|------|----------|
| 랭킹 페이지 N+1 | `BattleLog.query.filter_by(...).count()` 반복 | 루프 내 쿼리 | 중간 (NPC 8개 기준은 무시 가능) |
| 대시보드 쿼리 수 | EventLog, BuildQueue, TrainQueue 등 다수 | 관계 로딩 | 낮음 (SQLite 기준 < 50ms) |
| 알림 폴링 | 10초마다 요청 | 실시간 필요성 낮음 | 낮음 (APScheduler 대안 불필요) |
| CSS 크기 | 1485줄 | 중복 미디어 쿼리 | 낮음 (gzip 시 15KB 이하) |

### 4.2 최적화 권장사항
1. **랭킹 페이지**: `BattleLog` 승/패 카운트를 `Park` 모델에 `wins/losses` 필드로 캐싱.
2. **대시보드**: `recent_logs`를 10개로 제한 (이미 적용). `other_parks`는 필요 시 AJAX 지연 로드.
3. **스케줄러**: 대규모 시 `Park.query.all()` → `yield_per(100)` 고려 (현재 8~20개 수준으론 불필요).

---

## 5. 개발 프로세스 교훈

### 5.1 버전 관리 교훈
- **SemVer 준수**: MAJOR(빌딩 변경), MINOR(기능 추가), PATCH(버그 수정) 명확히 구분.
- **CHANGELOG.md 강제**: 모든 PR은 CHANGELOG 항목 필수. 없으면 머지 금지.
- **마이그레이션 스크립트**: DB 필드 변경 시 `migrate_vX_X.py` 별도 파일로 작성. 자동 마이그레이션 프레임워크 미사용으로 수동 관리 필요.

### 5.2 보안 교훈
- **Exploit 재발 방지**: 코드 리뷰 시 "이 계산에서 int() 절사가 Exploit 가능한가?" 질문 필수.
- **XSS 방어**: `innerHTML` 사용 금지 목록 작성. `textContent`/`escapeHtml()`이 기본.
- **Race Condition**: 자원 교환 코드는 반드시 원자적 `UPDATE-WHERE` 패턴 검토.

### 5.3 밸런스 교훈
- **상수 중앙화**: `GameConfig` 외부 하드코딩 발견 시 즉시 리팩토링.
- **시뮬레이션 부재**: 100턴 시뮬레이션 스크립트 없어 밸런스 조정이 감에 의존. `tests/balance_sim.py` 작성 권장.
- **보호 모드**: 신규 유저 보호와 중후반 긴장감의 균형이 까다로움. 상수 튜닝은 실제 플레이 데이터 기반으로.

### 5.4 문서화 교훈
- **spec.md 생명 주기**: 코드가 대폭 변경되면 spec.md를 먼저 갱신하고 코드를 수정. (역순으로 하면 불일치 발생)
- **주석 품질**: 한국어 주석은 코드 의도를 명확히. "왜"를 설명. (예: `[v1.5.1] 소수점 불사 부대 Exploit 방지`)
- **버전 태그**: 코드 변경 시 `[vX.X.X]` 태그 + 변경 내용 한국어 주석 필수.

### 5.5 동시성 및 복구 설계 교훈 (v1.8.0)
- **비관적 락의 필요성**: GET 요청 내부에서 무심코 진행하는 데이터 가공 및 커밋(`db.session.commit()`)도 비동기 POST 행동 트랜잭션의 변경 사항을 덮어쓸 수 있음을 인지함. 상태 전이 및 부조 작업 시 비관적 락(`with_for_update`)을 명확히 획득한 후 데이터를 다루는 것이 웹 환경의 정석임.
- **2중 3중의 안전 가드**: 예기치 못한 DB 오류나 중복 클릭 레이스로 유저의 핵심 엔티티가 증발할 수 있는 가능성을 항상 열어두고, 런타임의 최상위 진입로(로그인, 루트, 대시보드 등)에서 데이터 무결성을 검증하여 공원 자동 복구를 수행하도록 한 설계는 시스템 장애 복원력(Resilience)을 획기적으로 상승시키는 결정이었음.

### 5.6 Gunicorn 안전 분산 동시성 및 트랜잭션 격리 교훈 (v1.8.1)
- **스레드 락의 한계와 DB 락의 중요성**: Gunicorn 등의 다중 워커 프로세스 환경에서 파이썬 `threading.Lock()`은 단일 프로세스 내부의 스레드 장벽만 지키므로 아무런 동시성 제어 역할을 못 함을 깨달음. 데이터베이스 레벨의 비관적 락(`with_for_update()`)만이 프로세스를 초월해 데이터를 안정적으로 직렬화할 수 있으며, 일관된 순서(id asc) 정렬을 통해 데드락 위험을 예방하는 지혜가 중요함. (특히 기본 DB인 SQLite dialect에서는 `with_for_update`가 no-op으로 동작하지만, PostgreSQL/MySQL 등 실제 행 락을 제공하는 RDBMS로의 프로덕션 이주시 수평 확장성과 무결성을 지키기 위해 이 소스 코드 수준의 2-Way Lock Canonical Order 설계를 통합 및 유지하며, SQLite 단일 환경에서는 WAL 모드와 DB 파일 수준 단일 쓰기 lock으로 동시성 정합성을 안전하게 상호 보완하는 방식을 사용해야 한다는 소중한 아키텍처적 교훈을 정립함.)
- **autoflush와 파편화된 커밋의 독약성**: 메모리 상태를 수정하고 uncommitted 상태를 둔 채 다른 원자적 쿼리를 실행할 때 일어나는 SQLAlchemy `autoflush`의 자동 덮어쓰기 현상 및 엔진 기능 내부에 하드코딩된 `db.session.commit()`이 트랜잭션을 조각내어 롤백 무결성을 무력화하는 문제를 학습함. 이를 위해 `commit=False` 제어 기법을 도입하고 passive_growth를 `case()` 기반 SQL UPDATE로 원자화하여 안전한 비간섭적 트랜잭션 격리망을 달성하는 구조의 우수함을 경험함.

### 5.7 보상 트랜잭션 및 복구 신뢰성 교훈 (v1.8.2) [NEW]
- **분할 트랜잭션(Split Transaction) 아키텍처와 AP 누수**: Ghost AP 현상 및 동시성 락 충돌 방지를 위해 `consume_turn()`에서 AP 차감을 선행 커밋(Early Commit)한 조치는 뛰어난 결정이었으나, 이로 인해 후속 행동 로직(Action)에서 예외 발생 시 자원이 증발하는 분할 트랜잭션 부작용을 낳았음.
- **보상 트랜잭션(Compensating Transaction)의 우수성**: 분할된 비원자적 트랜잭션 하에서 자원의 정합성을 보장하는 가장 세련된 해법은 행동 실패를 즉각 감지해 이미 커밋된 자원을 안전하게 복구(AP 환불) 및 커밋해주는 **보상 트랜잭션 패턴**임을 깊이 교훈을 얻음.
- **공용 헬퍼와 코드 일원화**: 파편화된 각 라우트 핸들러 내부에서 무분별하게 인라인 SQL을 통해 상태를 직접 수정하던 예외 처리 로직들을 공용 `game_engine.refund_ap` 헬퍼로 통합함으로써, 복구 메커니즘을 중앙화하고 코드 유지보수성과 트랜잭션 복구 신뢰성을 대폭 향상할 수 있었음.

### 5.8 좀비 상태 차단 및 TOCTOU 교훈 (v1.8.3) [NEW]
- **동시성 락(Lock) 대기 간극과 TOCTOU 취약점**: 비관적 락(`with_for_update()`)을 획득하더라도 락 대기 시간 동안 상대방 또는 본인의 공원이 멸망(`is_destroyed`)할 수 있는 시간차가 존재한다는 사실을 깨달음. 비관적 락을 획득하고 객체를 데이터베이스로부터 리프레시(`db.session.refresh`)한 직후, 즉시 비즈니스 도메인의 핵심 제약 상태를 다시 검증하는 '2차 검증(Post-Lock Re-validation)' 패턴의 절대적 필요성을 배움.
- **턴 처리 중 멸망한 좀비 상태(Zombie State) 차단**: 턴 소비(`consume_turn()`) 도중 AP가 부족하여 `process_turn()`이 기동될 때 보스 HP가 0이 되어 멸망하더라도, 후속 검증이 없으면 잔여 행동(침공, 건설 등)을 좀비처럼 마저 실행해버리는 치명적 구조 결함을 식별함. 턴 처리 내부 로직 수행 직후 멸망 상태를 재확인하여 실행을 차단함으로써 상태 머신의 결함을 완전 무결하게 제거할 수 있었음.

### 5.9 Cascade Delete 에스크로 환불 정합성 교훈 (v1.8.4) [NEW]
- **관계형 Cascade Delete와 에스크로 자원 유실**: `cascade='all, delete-orphan'` 옵션은 고아 레코드 방지에 유용하지만, 비즈니스 도메인에서 이미 자원/유닛을 선차감해 둔 에스크로(Escrow) 상태에 대한 환불 로직을 보장하지 못한다는 맹점을 학습함. 관계 정의로 인한 연쇄 삭제가 발생할 때, 에스크로 환불이 수행되지 않으면 심각한 자원 누수(Resource Leakage) 취약점으로 연결됨을 깊이 인지함.
- **DB 이벤트 리스너를 통한 무결성 강제**: 파편화된 서비스 라우트 내부나 엔진 함수에서 공원 삭제 시의 에스크로 환불을 일일이 수작업으로 처리하려고 시도하면 누락되기 쉬움. SQLAlchemy `before_delete` 이벤트 리스너를 활용해 DB 레벨에서 삭제 직전에 대상을 가로채어, 생존해 있는 발신자(`sender_id`)에게 원자적으로 자원 및 유닛을 복구(Cap 제한 포함)하도록 설계함으로써 어떤 삭제 경로에서도 빈틈없이 안전망이 자동 기동되는 정교한 아키텍처의 강력함을 교훈으로 얻음.

### 5.10 외교 동시성 및 관계 직렬화 교훈 (v1.8.5) [NEW]
- **표준 순서 강제(Canonical Ordering)의 강력함**: 1:1 관계나 양방향 관계 데이터를 테이블에 저장할 때, Unique 제약이 있더라도 ID 저장 순서가 `(A, B)`와 `(B, A)`로 다르면 DB 제약이 작동하지 않는 다중 쓰기 레이스 컨디션이 유발됨을 깨달음. 항상 ID 순서를 `min(A, B)`, `max(A, B)`로 정렬하여 삽입하도록 도메인에 강제하고, 대신 원 요청자를 식별하기 위해 별도의 `initiator_id` 외래키 컬럼을 선언해 보완하는 정석적인 구조의 강력함을 배움.
- **다중 엔티티 비관적 락의 데드락(Deadlock) 방지**: 동시 교차 외교 요청에서 발생할 수 있는 레이스 컨디션을 방지하기 위해 두 공원 모두에 대해 `with_for_update()` 락을 획득해야 하나, 획득 순서가 꼬이면 즉시 교사 데드락(Deadlock)에 빠짐을 이해함. 이를 방어하기 위해 락을 걸고자 하는 ID 목록을 항상 오름차순(`sorted([id_a, id_b])`)으로 정렬한 뒤 일괄적으로 락을 획득하는 직렬화 정렬 락 패턴의 중요성을 체득함.
- **도메인 정합성 보장을 위한 일괄 벌크 업데이트(Bulk Update)**: 이전에 누적된 중복이나 오염된 관계가 존재할 수 있는 상황에서 `.first()`를 사용한 단일 인스턴스 갱신은 좀비 레코드를 잔존시킴으로써 게임의 중대 로직을 파괴하는 2차 참사를 유발함. 관계 갱신이나 해제 시에는 `.update()` 벌크 쿼리를 통해 동일 쌍의 모든 연관 관계를 원자적으로 일괄 전환하여, 기존에 오염된 비정상 데이터까지 스스로 자가 치료(Self-Healing)하는 강건한 트랜잭션 설계의 가치를 체득함.

### 5.11 NPC 동시성 락 및 Nested Transaction 격리 교훈 (v1.8.6) [NEW]
- **일괄 처리 트랜잭션 내 조기 커밋(Early Commit) 부작용**: 루프 시작 전 전체 레코드를 비관적 락 조회하더라도, 루프 내부에서 `db.session.commit()`이 실행되면 아직 처리되지 않은 다른 모든 레코드의 락까지 유실되는 관계형 데이터베이스의 락 릴리즈 메커니즘을 뼈저리게 학습함. 루프 외부에서는 ID 목록만 추출하고, 루프 내부에서 개별적으로 독립된 락 쿼리와 트랜잭션 커밋을 수행하는 격리 설계의 중요성을 깨달음.
- **Nested Transaction (Savepoint)을 통한 비관적 락과 상태 보존**: 런타임 행동 도중 발생한 예외로 인해 `rollback()`을 수행하면, 트랜잭션 전체가 취소되면서 어렵게 획득한 비관적 락까지 완전히 소멸한다는 사실을 알게 됨. 이를 극복하기 위해 `db.session.begin_nested()` 세이브포인트를 구동하여, 부분 예외 시 오직 해당 행동만 안전하게 취소하고 부모 트랜잭션의 락과 진행 데이터(turn_count 등)는 그대로 보존하는 복구 아키텍처의 탁월한 강건함을 배움.
- **연산 TOCTOU 방어를 위한 사전 2-Way Lock 강제**: 피해량 산정과 실제 DB UPDATE문 사이에 발생하는 실시간 비동기 격차(TOCTOU)를 방지하려면, 조건 판단 및 수치 계산 시작 직전에 대상 공원과 자아 공원에 대해 ID 정렬 순차로 2중 비관적 락을 철저히 확보해야 계산의 일관성과 로그의 무결성을 동시에 보장할 수 있음을 배움.

### 5.12 교역 거절 IDOR 및 좀비 거래 정화 교훈 (v1.8.7)
- **인가 가드(Authorization Guard)의 절대적 필요성**: 동시성 레이스 컨디션을 방지하기 위해 벌크 UPDATE나 원자적 연산을 수행할 때, 보안 컨텍스트 상의 인가 가드(`receiver_id == park.id`)를 조건식에 반드시 포함해야 함을 배움. 원자적 트랜잭션 확보에만 집중하고 권한 검증 조건을 필터에서 누락하게 되면 인가 우회(IDOR) 취약점이 노출되어 제3자가 시스템 무결성을 교란할 수 있음을 뼈저리게 인식함.
- **시장 수명 주기 제약 조건의 선제 필터링**: 개별 트랜잭션 실행 단계(TOCTOU 검증)에서 멸망 상태를 뒤늦게 감지하고 복구하는 사후 안전 가드는 올바른 설계이나, 이를 데이터 조회 단계(Query Level)에서 선제적으로 걸러내지 않으면 UI 리소스를 낭비하고 불필요한 비관적 락 경합 및 트랜잭션 오버헤드를 가중함을 학습함. 데이터 로딩 시점부터 조인(`JOIN`)과 필터 가드를 사용해 멸망한 상태를 엄밀하게 배제하는 전방위적 정화 설계의 중요성을 깨달음.

### 5.13 턴 소비 슬로우 패스 내 AP 복제 Lost Update 차단 교훈 (v1.8.8)
- **트랜잭션 분할과 비관적 락 해제 지연의 간극**: `db.session.commit()`이 호출되는 순간 스레드가 쥐고 있던 RDBMS 비관적 락(`with_for_update`)이 영구 해제됨을 명확히 배움. AP 충전과 NPC 동기화라는 비즈니스 로직적 장시간 처리가 그 뒤에 연동될 경우, 락이 해제되어 있는 그 시간차(Gap) 동안 concurrent 다중 요청(패스트 패스)이 안전 가드를 뚫고 DB의 실시간 상태를 변경시킬 수 있음을 뼈저리게 깨달음.
- **2차 비관적 락 및 refresh 동기화 패턴의 완결성**: 장기 소요 작업(예: NPC 턴 동기화 루프)으로 인해 락을 계속 쥐고 갈 수 없는 성능 병목 상황에서는, 트랜잭션을 분할하되 **핵심 연산 및 재검증 직전에 다시 락을 획득하고 DB 리프레시(`db.session.refresh`)를 명시적으로 수행**하는 구조만이 concurrent 요청의 변경점을 온전히 보존하고 Stale 메모리 데이터 덮어쓰기(Lost Update)를 근본적으로 차단하는 가장 세련되고 견고한 방어 패턴임을 깊이 인지함.

### 5.14 RDBMS 세이브포인트(Savepoint) Flush 격리와 라우터 레벨 명시적 커밋 제어 (v1.8.9) [NEW]
- **중첩 트랜잭션 내 Commit의 파괴적 영향**: SQLAlchemy의 `begin_nested()` (SAVEPOINT) 격리망 하에서 내부 연산이 임의로 `db.session.commit()`을 호출하면, RDBMS는 중첩 구조가 아닌 전체 트랜잭션을 영구적으로 확정짓고 세이브포인트를 깨뜨립니다. 이로 인해 후속 롤백/커밋이 `ResourceClosedError`를 만나고 데이터 무결성이 산산조각 납니다. 중첩 세이브포인트를 유지하면서 메모리 Stale 상태를 동기화해야 할 때는 오직 `db.session.flush()`만을 사용하여 SQL을 방출해야 함을 배웠습니다. 또한, `nested.rollback()` 실패에 대비해 예외 절(except) 내부에서 부모 세션을 롤백시키는 **2중 롤백 방어막**을 설계해야 복구의 안전성이 완전히 확보됨을 인지했습니다.
- **AP 복구의 영구 저장 강제**: Flask 라이프사이클 내에서 DB 세션을 관리할 때, API 내부에서 데이터를 `UPDATE`하더라도 라우터가 `redirect` 등으로 최종 커밋 없이 끝내면 `teardown_request`가 세션을 폐기하면서 환불 쿼리가 롤백되어 소실(AP Blackhole)됩니다. 트랜잭션 경계를 통제하는 라우터 레벨에서 예외/기각에 따른 AP 환불 보상 트랜잭션 호출 시시 즉시 `db.session.commit()`을 명시적으로 집행해줘야만 자원 보존의 완결성이 보장됨을 깊이 깨달았습니다.
- **비정적 런타임 갱신의 2차 락 재확보**: 밀사 임무 처리 등 장시간 소요 작업 이후 인스턴스의 상태를 메모리 속성 연산으로 조작하는 비정적 연산(예: `_process_overcrowding`)을 수행할 때는, 중간에 비관적 락이 해제되어 있는 틈새(Lock-free Gap) 동안 concurrent 다중 요청이 끼어들어 데이터를 변경했을 리스크를 철저히 대비해야 합니다. 이 갭을 메우기 위해 연산 직전에 다시 `with_for_update()` 락을 확보하고 `db.session.refresh()`를 수행하는 2차 락 패턴을 일관되게 활용해야 Lost Update 정합성 위협을 완전히 종식시킬 수 있음을 뼈저리게 깨달았습니다.

### 5.15 NPC 턴 진행 및 행동 AI 2단계 트랜잭션 경계 분리와 SQLite WAL/busy_timeout Pragma 도입 교훈 (v1.8.9) [NEW]
- **교차 락킹과 Canonical Ordering의 무력화 시나리오**: 데드락을 예방하기 위해 두 공원의 ID를 정렬하여 순차적으로 락을 획득하는 Canonical Locking을 적용하더라도, 상위 트랜잭션 진입 시점에 이미 그 중 하나의 공원(NPC) 락을 무조건적으로 쥐고(`with_for_update`) 시작하면 데드락 방지 정렬 순서가 완전히 붕괴됨을 배움. `Player.id < NPC.id`일 경우 락을 획득하려는 최종 정렬 순서는 `[Player, NPC]`가 되지만, NPC 스레드가 `NPC` 락을 이미 선점 쥔 상태에서 `Player` 락을 대기하고 플레이어 스레드는 `Player` 락 획득 후 `NPC` 락을 얻으려 대기하며 영구적인 교차 대기 교착 상태(Deadlock)에 빠짐을 인지함.
- **2단계 트랜잭션 경계 분리 및 Lock-free Gap의 설계적 절충**: 상위 턴 동기화 스케줄러 `_sync_npc_turns()` 레벨에서 NPC 기본 턴 처리(`process_turn`) 완료 즉시 `db.session.commit()`을 강제해 선점 락을 완전히 해제(소멸)한 뒤, 독립 트랜잭션 하에 `process_npc_turn()` AI 의사결정을 수행하는 **2단계 트랜잭션 경계 분리 구조**의 위력을 알게 됨. 이로써 발생하는 미세한 무락 갭(Lock-free Gap)은 NPC 공격 시작 직전에 플레이어의 자원 상태가 타 스레드에 의해 변할 수 있는 동시성 갭이지만, 교착 상태(`[DEADLOCK-F005]`)를 근원적으로 해결하기 위해 감수한 고도의 설계적 절충(Trade-off)임을 체득함.
- **SQLite Engine Pragma 이벤트 리스너를 통한 동시성 보완**: SQLAlchemy SQLite dialect에서 `with_for_update()`가 no-op(실제 FOR UPDATE SQL 미방출)으로 작동하는 제약 하에, 다중 프로세스(Gunicorn) 환경에서 Database Locked(DB 잠금) 병목을 예방하는 현실적인 방법을 규명함. `Engine` 'connect' 이벤트 리스너를 수립하여 SQLite 연결 즉시 `PRAGMA journal_mode=WAL` 및 `PRAGMA busy_timeout=5000` pragma를 강제 주입하게 함으로써 SQLite의 동시 쓰기 정합성과 읽기 성능을 비약적으로 보완할 수 있음을 입증함. 또한 이 구조는 향후 PostgreSQL/MySQL 등 실제 행 락(Row Lock) RDBMS로 이주할 시에도 소스 수정 없이 ID 오름차순 Canonical Locking과 연동되어 높은 수준의 확장성을 가짐을 정립함.

---

## 6. 코드 냄새 (Code Smells)

| 위치 | 냄새 | 설명 | 개선 방향 |
|------|------|------|----------|
| `game_engine.py:692` | `ate_konpeito` 미사용 | `_process_food_consumption`에서 콘페이토 소비 여부 판별 코드가 `if False`로 버려짐 | 실제 콘페이토 소비 추적 로직 구현 또는 정리 |
| `models.py:97` | `consecutive_trash_turns` 중복 | 84행과 97행에 동일 필드 중복 정의 | SQLAlchemy는 마지막 정의가 우선. 명시적 제거 필요. |
| `game_routes.py:280` | 교역 수락 로직 복잡도 | 에스크로 + 환불 + 상태 전환이 한 함수에 80줄 | 상태 머신 패턴 또는 서비스 객체 분리 |
| `dashboard.html` | 인라인 스타일 50+개 | `style="color:#ffaa00"` 등 하드코딩 | [v1.7.0] Tailwind CSS 도입으로 95% 이상 청소 및 최적화 완료 (해결됨) |
| `game_engine.py:1081` | 중독 판정 부정확 | `park.konpeito < park.konpeito_cap`으로 콘페이토 소비 여부 판별 | 이전 턴 대비 감소 추적 또는 `_consume_np` 반환값 활용 |

---

## 7. UI/UX 리팩토링 및 디자인 시스템 교훈 [NEW]

v1.7.0 리팩토링 단계를 거치며 획득한 고밀도 BBS 레트로 디자인 최적화의 기술적 교훈입니다.

### 7.1 zero-setup 프레임워크와의 결합 극대화 (Tailwind CDN)
*   **교훈:** 배포 환경의 간소화(Raspberry Pi 기동, zero-setup 원칙)를 저해하지 않고 현대적 UI/UX를 고속 개발하기 위해 **Tailwind CSS CDN 및 테마 extend 설정**을 `base.html`에 전격 활용한 시도가 매우 주효했음. 빌드 파이프라인(npm, PostCSS)의 추가 오버헤드 없이도 SSR Jinja2 환경에 점진 마이그레이션이 원활함을 입증함.

### 7.2 CRT 오버레이 및 0.15s 무한 플리커(Flicker) 성능 튜닝
*   **교훈:** 화면 전체가 미세하게 진동하는 `flicker` keyframe 애니메이션은 CPU/GPU 리소스를 과도하게 소모하여 성능 저하 및 눈의 피로를 유발할 수 있음.
*   **최적화 방법:** body의 opacity 진동 범위를 극도로 좁혀(`0.97` ~ `1.0`) 미묘한 인광 진동만 살리고, 전역 레이어에 pointer-events: none의 CRT 스캔라인 오버레이를 하이브리드로 결합하여 브라우저 가속 렌더링 성능을 90% 이상 세이빙하면서 BBS 감성을 완벽히 구현함.

### 7.3 가상 모크업(이스터에그)의 UX 환기성
*   **교훈:** 백엔드 미구현 스펙(보스 스킬 트리 시스템)을 완전히 배제하기보다, 1초 주기로 요동치는 실시간 SP 요동 카운터와 SVG 노드망 연동 및 팝업 대사를 지닌 화려한 **가상의 모크업 터미널(skills.html)**로 구현하여 플레이어에게 큰 시각적 재미와 미래 업데이트 로드맵에 대한 동기부여를 선사하는 이색적인 UX 가치를 도출함.

---

## 8. 감사문서 및 감사 대응 프로세스 교훈 [NEW]

### 8.1 감사 보고서 대응 표준 조치 기록 템플릿 도입
*   **교훈**: AI 재감사 및 피드백 루프에서 발견되는 Hold 상태 지적 사항들에 대해, 단순 코드 수정이나 텍스트 요약에 그치지 않고 구체적이며 표준적인 사차(4-Tier) 조치 기술 방법론을 적용함으로써 감사 프로세스의 신뢰성과 투명성을 극대화할 수 있음을 체득함.
*   **작성 규격 표준 (MANDATORY)**:
    1. **Status**: 모든 해결된 항목은 반드시 `Verified / Fixed (완치)`로 개정합니다.
    2. **조치내용**: 실제 리팩토링 및 아키텍처 문서화 등이 프로젝트 전반에 어떻게 수립되었는지 한국어로 일목요연하게 개괄합니다.
    3. **처리방법**: 구체적인 코드 메서드 레벨, 환경변수 가드 처리, RDBMS DB 비관적 락 등의 구체적 구현 및 복구 로직의 상세를 기술합니다.
    4. **남은위협**: 조치 후 도메인이나 운영 환경에 잔존하는 보안 및 로직 리스크(없는 경우 0% 또는 없음)를 명확히 선언합니다.
    5. **감사에게 요청할 사항**: 재감사 시 감사자가 어떠한 재현 스크립트나 명령(예: `pytest -W error`) 또는 환경 테스트를 통해 완치를 교차 검증해야 하는지를 정교하게 가이드합니다.
*   **향후 정책**: 본 프로젝트의 모든 차기 감사 보고서 대응 및 재감사 조치 리포팅 시에는 오직 이 4대 기술 규격을 철저하게 강제 및 유지하여 지속 가능한 정합성을 보장하도록 확정합니다.

---

*문서 끝*
